# Testing

> Bridge — canonical standard is not here.

**Canonical:** [.agents/documents/testing.md](../../testing.md) (Vitest 4, `forks` pool, 4-concern rule, file naming, fixtures).

This `application/testing/` will hold **per-module test plans and coverage notes** as they are authored. Until then:

- Standard: [testing.md](../../testing.md) §1–9
- Global rules: `~/.agents/rules/test-architecture.md` + `development-quality.md`
- Layout: server tests `src/**/tests/` (mirrored), UI `src/dashboard/ui/src/**/__tests__/`

Do not duplicate the standard — link to it.
