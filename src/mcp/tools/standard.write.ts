/**
 * standard.write — thin re-exporter.
 *
 * All implementation moved to `standard-write/` sub-modules.
 * This file exists for backward compatibility.
 */

export { handleStandardWrite } from "./standard-write/index.js";
