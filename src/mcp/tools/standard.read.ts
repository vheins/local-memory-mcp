/**
 * standard.read — thin re-exporter.
 *
 * All implementation moved to `standard-read/` sub-modules.
 * This file exists for backward compatibility.
 */

export { handleStandardRead } from "./standard-read";
