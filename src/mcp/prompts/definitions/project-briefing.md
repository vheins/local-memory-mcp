---
name: project-briefing
description: Contextual onboarding to current repository.
arguments: []
agent: Session Concierge
---

## Project Briefing

Load tasks (in_progress, pending), handoffs (pending), memory (agent-context OR memory-search), standards (standard-search). Summarize active context. Propose next steps based on active queue priority (1=Low..5=Critical).

For detailed FSM execution (S0→S7 with guards), load the `project-briefing` skill.
